# 高斯瓦片点击拾取流程（函数级）

本文档只描述 **CPU 扫描**路径：`GaussianSplatPrimitive.prototype.getSplatInfoAtScreenPosition` 如何从一次鼠标点击计算出 **aggregateIndex / plyIndex**，以及这些索引如何驱动选中/锁定/颜色等状态更新。

> 说明：代码中存在 GPU picking 相关宏（如 `PICK_PASS`）与资源字段（如 `_pickFramebuffer`），但本文档按需求 **不展开 GPU picking 链路**。

## 术语与索引

- **aggregateIndex（聚合索引）**
  - 定义：把当前参与渲染的 tiles（`tileset._selectedTiles`）的点按 tile 顺序拼接后的 **连续索引**。
  - 范围：`0 .. _numSplats - 1`
  - 用途：`setSplatSelection()` / `setSplatLock()` / 颜色组分配等核心状态均使用它。

- **plyIndex（PLY 索引）**
  - 定义：原始 PLY 文件里的点索引（可能不连续、跨 tile）。
  - 获取方式：在拾取返回阶段，通过 `plyIndex = _plyIndicesAggregate[aggregateIndex]` 取得。

## 关键数据结构（都在 `GaussianSplatPrimitive` 上）

- **`_positions`**：Float 数组，三元组 (x,y,z)，存储在 **root space**（相对 `_rootTransform`）。
- **`_rootTransform`**：root space→tileset model space 的变换（3D Tiles root.transform）。
- **`tileset.modelMatrix`**：tileset model space→world space 的变换。
- **`_numSplats`**：聚合后的点总数（aggregateIndex 的上界）。
- **`_plyIndicesAggregate`**：长度为 `_numSplats` 的 `Uint32Array`，每个 aggregateIndex 对应一个 plyIndex。
- **`_plyIndexToAggregateIndex`**：`Map<number, number>`，plyIndex → aggregateIndex（用于“按 plyIndex 反查聚合索引”）。
- **`_selectedSplatIndices`**：`Set<number>`，记录被选中的 aggregateIndex。
- **`_lockedSplatIndices`**：`Set<number>`，记录被锁定的 aggregateIndex。
- **`_needsStateTextureUpdate/_dirty`**：状态变化标记（触发状态纹理更新与重绘）。

## 函数级流程总览

```mermaid
flowchart TD
  ClickEvent[ScreenSpaceEventHandler_LeftClick] --> CallPick[primitive.getSplatInfoAtScreenPosition(scene,windowPosition,options)]
  CallPick --> Validate[参数与_positions检查]
  Validate --> BuildCandidates[生成candidateIndices]
  BuildCandidates --> WindowToDB[SceneTransforms.transformWindowToDrawingBuffer]
  WindowToDB --> PickPos[scene.pickPosition(windowPosition)?]
  PickPos --> RootToWorld[计算rootToWorldMatrix]
  RootToWorld --> ScanLoop[循环候选点:rootToWorld+投影+距离]
  ScanLoop --> ChooseIndex[选finalIndex]
  ChooseIndex --> BuildResult[plyIndex=_plyIndicesAggregate[finalIndex]]
  BuildResult --> ReturnInfo[返回{aggregateIndex,plyIndex,isSelected,isLocked,...}]
  ReturnInfo --> ApplyState[业务层决定: setSplatSelection/setSplatLock 等]
```

## 1. 应用侧（点击入口）

典型入口是注册点击事件后调用 primitive 的拾取函数（示例在 `高斯瓦片点击选中功能完整指南.md` 中给出）：

- 点击事件：`ScreenSpaceEventHandler.setInputAction(LEFT_CLICK, handler)`
- 在 handler 内：调用 `tileset.gaussianSplatPrimitive.getSplatInfoAtScreenPosition(viewer.scene, click.position, options)`

建议 options 明确传入：
- `searchSelectedOnly`：默认 `true`
- `maxDistance`：像素阈值（drawingBuffer space）
- `worldMaxDistance`：世界空间阈值（米，依赖 `pickPosition`）

## 2. 引擎侧：`getSplatInfoAtScreenPosition`（核心）

函数位置：`packages/engine/Source/Scene/GaussianSplatPrimitive.js`

### 2.1 参数与数据检查

满足任一条件会直接返回 `undefined`：
- `scene` 或 `windowPosition` 为空
- `_positions` 不存在或长度为 0

### 2.2 候选集 `candidateIndices` 的生成（决定性能）

候选集选择优先级如下：

1. **只搜已选中**（默认行为）  
   当 `searchSelectedOnly === true` 且 `_selectedSplatIndices.size > 0`：  
   `candidateIndices = Array.from(_selectedSplatIndices)`

2. **外部指定候选集**  
   当 `options.indices` 存在且长度 > 0：  
   `candidateIndices = options.indices`

3. **搜索全量**（`searchSelectedOnly === false`）  
   当 `_numSplats > 0` 时：  
   `candidateIndices = Array.from({ length: _numSplats }, (_, i) => i)`  

> 性能结论：第 3 种会导致 **O(_numSplats)** 的点击计算，每个点还要做矩阵变换与投影，百万级点数会出现明显卡顿。

关键代码片段（候选集分支）：

```js
// 省略参数解析...
let candidateIndices;
if (searchSelectedOnly && this._selectedSplatIndices.size > 0) {
  candidateIndices = Array.from(this._selectedSplatIndices);
} else if (defined(options) && defined(options.indices) && options.indices.length > 0) {
  candidateIndices = options.indices;
} else if (!searchSelectedOnly) {
  if (this._numSplats > 0) {
    candidateIndices = Array.from({ length: this._numSplats }, (_, i) => i);
  } else {
    candidateIndices = undefined;
  }
}
```

### 2.3 坐标准备：window→drawingBuffer 与 pickPosition

1) 把点击 window 坐标转换为 drawingBuffer 坐标（用于像素距离）：
- `SceneTransforms.transformWindowToDrawingBuffer(scene, windowPosition, scratchPickDrawingBuffer)`

失败则返回 `undefined`。

2) 尝试从深度获取世界坐标（可选，用于 worldMaxDistance）：
- `pickWorldSupported = scene.pickPositionSupported === true`
- `pickWorldPosition = scene.pickPosition(windowPosition, scratchPickWorldPosition)`（可能为 `undefined`）

> 注意：`pickPosition` 可能出现“穿透/高度不准”的情况，它不影响像素距离路径，但会影响 worldMaxDistance 的筛选效果。

### 2.4 统一空间：root space → world space

`_positions` 在 root space，需要转换到 world space 才能投影与计算世界距离：

- `rootToWorldMatrix = tileset.modelMatrix * this._rootTransform`
- fallback：如果缺少矩阵，使用 `Matrix4.IDENTITY`（假设 positions 已经在世界空间）

关键代码片段：

```js
const tileset = this._tileset;
let rootToWorldMatrix;
if (defined(this._rootTransform) && defined(tileset.modelMatrix)) {
  rootToWorldMatrix = Matrix4.multiply(
    tileset.modelMatrix,
    this._rootTransform,
    scratchMatrix4A,
  );
} else {
  rootToWorldMatrix = Matrix4.IDENTITY;
}
```

### 2.5 扫描候选点：投影 + 距离（CPU 计算最重的一段）

对每个 `index`（aggregateIndex）：

1. 从 `_positions` 取 root space 坐标：
   - `base = index * 3`
   - `(x,y,z) = _positions[base..base+2]`

2. root→world：
   - `worldPosition = Matrix4.multiplyByPoint(rootToWorldMatrix, rootPos, scratchPickWorldPosition)`

3. world→drawingBuffer 投影：
   - `projected = SceneTransforms.worldToDrawingBufferCoordinates(scene, worldPosition, scratchPickProjected)`
   - 投影失败：跳过该点

4. 像素距离筛选：
   - `distance = sqrt((projected.x - db.x)^2 + (projected.y - db.y)^2)`
   - 若 `distance <= closestDistance`，更新 `closestIndex`

5. 世界距离筛选（可选，依赖 pickWorldPosition）：
   - `worldDistance = Cartesian3.distance(pickWorldPosition, worldPosition)`
   - 若 `worldDistance <= closestWorldDistance`，更新 `closestWorldIndex`

### 2.6 最终索引选择与返回值

- 优先使用像素距离找到的 `closestIndex`  
  如果像素没找到（`closestIndex === -1`）但世界距离找到了（`closestWorldIndex !== -1`），则回退使用世界距离结果。

- `plyIndex` 的来源：
  - `plyIndex = _plyIndicesAggregate[finalIndex]`（若 `_plyIndicesAggregate` 存在）

最终返回结构：

```js
{
  aggregateIndex: finalIndex,
  plyIndex: plyIndex,
  colorGroupId: this._splatColorGroups.get(finalIndex) ?? 0,
  isSelected: this._selectedSplatIndices.has(finalIndex),
  isLocked: this._lockedSplatIndices.has(finalIndex),
  pixelDistance: closestDistance,
}
```

## 3. `plyIndex` 数据从哪里来（聚合构建阶段）

`_plyIndicesAggregate` 与 `_plyIndexToAggregateIndex` 的构建发生在 `update(frameState)` 中，当 tileset 的选中 tile 数量变化时触发重建。

核心步骤：

1) 遍历 `tileset._selectedTiles`，按 tile 顺序分配 aggregateIndex 区间  
2) 若 tile.content 提供 `plyIndices` 且长度与 `pointsLength` 一致：  
   - 用 `plyIndices[i]` 作为真实 plyIndex  
   - 同步建立 `plyIndex → aggregateIndex` 映射  
3) 否则回退：连续索引假设（可能与真实 PLY 不一致）

这也是“为什么有时能拿到 aggregateIndex，但 plyIndex 不对/不稳定”的根源之一：**取决于 tile.content 是否真的带了可靠的 `plyIndices` 数据。**

## 4. 点击后如何触发“选中/锁定/颜色变化”

拾取只返回 info，不会自动改状态。业务层通常按 info 做：

- 选中：
  - `setSplatSelection([info.aggregateIndex], true/false)`
  - 或 `setSplatSelectionByPlyIndex([info.plyIndex], true/false)`（内部用 `_plyIndexToAggregateIndex` 做转换）

- 锁定：
  - `setSplatLock([info.aggregateIndex], true/false)`
  - 或 `setSplatLockByPlyIndex([info.plyIndex], true/false)`

这些方法都会：
- 更新 `_selectedSplatIndices` / `_lockedSplatIndices`
- 设置 `this._needsStateTextureUpdate = true`
- 设置 `this._dirty = true`
- 选中时还会 `_markBoundaryDirty()`（轮廓 rings 边界缓存相关）

## 5. 常见 `undefined` 原因（按代码分支）

1. **参数无效**：`scene/windowPosition` 为空  
2. **位置数据缺失**：`_positions` 不存在或为空（tileset 未 ready / 未选中 tile / 正在重建）  
3. **候选集为空**：\n
   - `searchSelectedOnly=true` 但 `_selectedSplatIndices` 为空\n
   - 没传 `options.indices`\n
   - `searchSelectedOnly=false` 但 `_numSplats===0`\n
4. **drawingBuffer 坐标转换失败**：`transformWindowToDrawingBuffer` 返回 `undefined`  
5. **全部投影失败或距离全超阈值**：\n
   - `worldToDrawingBufferCoordinates` 大量返回 `undefined`（视锥外等）\n
   - 所有点的像素距离 > `maxDistance`，且（若有）世界距离 > `worldMaxDistance`\n

## 6. 性能瓶颈说明（为什么全量会卡）

当 `searchSelectedOnly=false` 且没有 `options.indices` 时，会生成 `_numSplats` 个候选并全量扫描。\n
每个点至少包含：\n
- root→world 的 `Matrix4.multiplyByPoint`\n
- world→drawingBuffer 的 `SceneTransforms.worldToDrawingBufferCoordinates`\n
- 距离计算（像素/世界）\n

因此点击复杂度近似为 **O(_numSplats)**，百万级 splat 会出现“点击卡几秒”的现象。

### 实用建议（不改代码前提）
- **优先用 `searchSelectedOnly:true`**（先通过其它方式缩小选中集合，再点击拾取）\n
- **尽量传 `options.indices`**（由业务侧维护一个“可能命中的点集合”，避免全量）\n
- **收紧 `maxDistance/worldMaxDistance`**（减少误命中，但注意过小会拾取不到）\n

