## 背景与目标

你当前的点击拾取（CPU 路径）在 `searchSelectedOnly=false` 时会走 **全量候选**：
- 生成 `candidateIndices = 0.._numSplats-1`
- 对每个 splat 做 **root→world 变换** + **world→drawingBuffer 投影** + **距离比较**

这条链路的复杂度接近 **O(N)**，对百万级点位会出现“点击卡几秒”。

本文档给出“**必须覆盖全量点位**（不依赖已选中集合/不依赖外部 indices）”的优化方案，分三大类：
- **A 类：CPU 仍逐点扫描**（算法形态仍是全量遍历，但减少卡顿/提升速度，改动小）
- **B 类：空间索引/加速结构**（逻辑仍覆盖全量点，但点击不再逐点扫描，改动中到大）
- **C 类：GPU picking（对照）**（不走 CPU 全量扫描，性能最佳，改动中偏大）

每个方案包含：**优缺点**、**改动程度**、**涉及文件**、**接入点（函数级）**。

> 基线实现与流程详见：`高斯瓦片点击拾取流程（函数级）.md`。

---

## 现状基线（必须先理解的瓶颈点）

### 基线入口与热路径

- 拾取入口：`GaussianSplatPrimitive.prototype.getSplatInfoAtScreenPosition(scene, windowPosition, options)`
- 全量候选分支：当 `!searchSelectedOnly` 且无 `options.indices` 时，生成 `0.._numSplats-1`
- 热循环：对每个 index 做：
  - `Matrix4.multiplyByPoint(rootToWorldMatrix, rootPos, ...)`
  - `SceneTransforms.worldToDrawingBufferCoordinates(scene, worldPos, ...)`
  - `sqrt(dx*dx + dy*dy)` 与阈值比较
  - 可选：`Cartesian3.distance(pickWorldPosition, worldPos)` 与阈值比较

### 现状文件/代码位置

- 主要逻辑：`packages/engine/Source/Scene/GaussianSplatPrimitive.js`（约 `1632-2033`）
- PLY 索引聚合与映射：同文件（约 `4342-4476`）

---

## 方案 A：CPU 仍逐点扫描（改动小）

这一类方案的共同特点：
- **仍然扫描所有点位**（最符合“必须全量”的字面要求）
- 但通过减少分配/减少昂贵运算/避免阻塞 UI 来改善体验

### A1. 取消每次点击创建全量数组（减少内存与 GC）

- **做法**：
  - 不再 `candidateIndices = Array.from({length:_numSplats},...)`
  - 改为直接 `for (let index = 0; index < this._numSplats; index++) { ... }`
  - 或缓存一份 `_allIndices`（`Uint32Array`）仅在 `_numSplats` 变化时重建

- **优点**：
  - 代码改动很小
  - 立刻减少一次点击的内存分配与 GC 抖动

- **缺点**：
  - 时间复杂度仍是 O(N)，点数大时仍可能慢（但通常会比“先建大数组再扫”更平滑）

- **改动程度**：小
- **涉及文件**：`packages/engine/Source/Scene/GaussianSplatPrimitive.js`
- **接入点**：`getSplatInfoAtScreenPosition` 的候选集分支（现有 `1705-1724` 附近）

---

### A2. 距离比较用“平方距离”避免大量 sqrt（提升纯计算吞吐）

- **做法**：
  - 把 `distance = sqrt(dx*dx + dy*dy)` 改为比较 `dx*dx + dy*dy <= maxDistance*maxDistance`
  - `closestDistance` 改为存“最小平方距离”
  - 返回时如果需要 `pixelDistance` 再 `sqrt(minSq)`（只做一次）

- **优点**：
  - 在超大循环里，减少大量 `Math.sqrt`，通常能获得可观的纯计算提速
  - 改动仍很小

- **缺点**：
  - 仍是 O(N)，只是常数更小

- **改动程度**：小
- **涉及文件**：`packages/engine/Source/Scene/GaussianSplatPrimitive.js`
- **接入点**：扫描循环内的像素距离计算（现有 `1878-1891` 附近）

---

### A3. “分帧/异步全量扫描”避免点击卡死（体验优化，时间不一定减少）

- **做法（两种）**：
  - **分帧扫描**：把循环拆成多段，每帧处理固定数量（例如 50k），直到找到结果或扫完
  - **Worker 扫描**：把必要数据（positions、rootToWorldMatrix、相机矩阵、viewport、clickPos）拷贝到 WebWorker，后台算出结果回主线程

- **优点**：
  - 用户体验显著提升：不再“点击卡住几秒”，而是“点击后稍等得到结果”
  - 适合“必须全量且点数巨大”的硬约束场景

- **缺点**：
  - API 形态通常要变：同步返回 `object|undefined` 变为 `Promise<object|undefined>` 或回调（业务层要跟着改）
  - Worker 方案需要复制/传输数据（内存、序列化成本）
  - 仍然需要做全量投影与计算，总耗时不一定变短，只是把阻塞变为异步

- **改动程度**：中（因为会影响 API/调用方与状态机）
- **涉及文件**：
  - `packages/engine/Source/Scene/GaussianSplatPrimitive.js`（新增状态机/任务队列）
  - （可选）新增 `packages/engine/Source/Workers/gaussianSplatPicker.js`（或类似）用于 Worker 执行
  - 业务侧调用处（如果你仓库外有应用代码）
- **接入点**：
  - `getSplatInfoAtScreenPosition`：改为触发任务并异步返回

---

### A4. 用相机矩阵手写投影，减少 `SceneTransforms.worldToDrawingBufferCoordinates` 开销（高风险的常数优化）

- **做法**：
  - 用 `camera.viewMatrix` + `camera.projectionMatrix`（或 `scene.context.uniformState`）把 worldPosition 投影到 clip/NDC，再转到 drawingBuffer 像素
  - 避免每点调用 `SceneTransforms.worldToDrawingBufferCoordinates`（它内部会处理更多场景/模式）

- **优点**：
  - 如果 `SceneTransforms` 路径开销大，可能带来明显提速

- **缺点**（重点）：
  - 风险较高：Cesium 的 SceneTransforms 需要兼容不同 `SceneMode`、眼偏移、viewport 等细节
  - 容易出现“拾取偏移/2D 模式异常/某些相机参数不一致”等 bug

- **改动程度**：中
- **涉及文件**：`packages/engine/Source/Scene/GaussianSplatPrimitive.js`
- **接入点**：循环内投影处（现有 `1866-1875`）

---

## 方案 B：空间索引 / 加速结构（改动中到大，点击不再逐点扫描）

这一类方案的共同点：
- 覆盖全量点位（索引构建时包含全量点）
- **点击时只查询少量候选 K**（\(K \ll N\)），然后复用现有精筛逻辑（像素/世界距离）

### B1. 八叉树 Octree（root space）+ 射线圆柱/膨胀 AABB 查询（推荐的通用方案）

- **核心思路**：
  - 在 root space 对 `_positions` 建一个 Octree（一次性或 tileset 更新时重建）
  - 点击时用相机射线（world）→ 变换到 root space
  - 用 “AABB 膨胀 r（≈worldMaxDistance）” 的 ray-AABB 交测试裁剪，快速收集候选叶子点
  - 对候选点再执行现有的“投影 + 像素距离/世界距离”精筛

- **优点**：
  - 点击复杂度从 O(N) 降到近似 O(log N + K)
  - 与现有 `worldMaxDistance` 很契合（可以作为“射线附近半径”）
  - root space 建树：tileset.modelMatrix 变化不需要重建树（只需更新矩阵）

- **缺点**：
  - 需要额外内存（节点 + 索引列表）
  - 构建可能耗时（但通常比每次点击全量扫更可接受）
  - 参数需要调：leafCapacity、maxDepth、radius（worldMaxDistance）

- **改动程度**：中到大
- **涉及文件**：
  - 必改：`packages/engine/Source/Scene/GaussianSplatPrimitive.js`（管理 octree 生命周期 + 接入拾取）
  - 建议新增：`packages/engine/Source/Scene/GaussianSplatOctree.js`（octree 构建/查询实现）
- **接入点**：
  - 在 `getSplatInfoAtScreenPosition` 的候选集分支中：当 `!searchSelectedOnly` 时用 octree 生成 `candidateIndices`
  - 在 `update(frameState)`：当 `_positions/_numSplats` 重建时标记 octree dirty 并重建

#### B1 的“可直接落地”实现步骤（更细）

##### Step 1：新增 Octree 模块（单独文件，便于维护）

- **新增文件**：`packages/engine/Source/Scene/GaussianSplatOctree.js`
- **职责**：
  - `constructor(positionsRootSpace, count, options)`
  - `queryRayInflatedAabb(rayOriginRoot, rayDirRoot, radius, outIndices, maxCandidates)`
  - `destroy()`（可选）

> 建议在 **root space** 建树（使用 `GaussianSplatPrimitive._positions`），这样 tileset 的 `modelMatrix` 变化时不需要重建树。

##### Step 2：在 `GaussianSplatPrimitive` 增加成员与生命周期

- **修改文件**：`packages/engine/Source/Scene/GaussianSplatPrimitive.js`
- **新增字段**（构造函数里）：
  - `this._splatOctree = undefined;`
  - `this._splatOctreeDirty = true;`
  - `this._splatOctreeOptions = { leafCapacity: 2048, maxDepth: 12, maxCandidates: 200000 };`（可调）

- **标记 dirty 的时机**：
  - 当 `update(frameState)` 中发生 `_positions/_numSplats` 重建（你现在就在 tileset 选中 tiles 变化时重建）时：`this._splatOctreeDirty = true;`

##### Step 3：构建 Octree（懒构建，避免每帧建）

- **新增方法**：`_buildSplatOctreeIfNeeded()`
  - 条件：`_splatOctreeDirty && defined(_positions) && _numSplats>0`
  - 行为：`this._splatOctree = new GaussianSplatOctree(this._positions, this._numSplats, this._splatOctreeOptions); this._splatOctreeDirty=false;`

##### Step 4：点击时生成射线并变换到 root space

在 `getSplatInfoAtScreenPosition(scene, windowPosition, options)` 中：
- `rayWorld = scene.camera.getPickRay(windowPosition, scratchRay)`
- 复用现有 `rootToWorldMatrix = tileset.modelMatrix * this._rootTransform`
- 计算 `worldToRootMatrix = inverse(rootToWorldMatrix)`
- `originRoot = Matrix4.multiplyByPoint(worldToRootMatrix, rayWorld.origin, scratchOriginRoot)`
- `dirRoot = Matrix4.multiplyByPointAsVector(worldToRootMatrix, rayWorld.direction, scratchDirRoot)` 并 normalize

##### Step 5：用 Octree 查询候选（替代全量 candidateIndices）

当 `!searchSelectedOnly` 且未提供 `options.indices` 时：
- `this._buildSplatOctreeIfNeeded()`
- `candidateIndices = this._splatOctree.queryRayInflatedAabb(originRoot, dirRoot, worldMaxDistance, scratchCandidateArray, maxCandidates)`

然后 **复用你现有的精筛循环**（root→world、投影、像素距离/世界距离对比），只不过候选从 N 变成 K。

##### Step 6：降级策略（避免极端情况下候选爆炸）

如果 `candidateIndices.length` 超过 `maxCandidates`：
- **方案**：直接降级到 GPU picking（如果启用）或提示收紧 `worldMaxDistance`
- **原因**：在极高密度/极大 radius 的情况下，Octree 会返回过多候选，退化为近似全量

---

### B2. 均匀网格（Uniform Grid / Spatial Hash）（比 Octree 更简单，场景密度均匀时很强）

- **核心思路**：
  - 在 root/world 空间建立 3D 网格（cellSize 以米为单位）
  - 每个 cell 保存一串 aggregateIndex
  - 点击时用 `pickWorldPosition` 或射线，查询附近 cell（半径 r）得到候选 K

- **优点**：
  - 实现相对简单，查询非常快（哈希 + 近邻 cell）
  - 如果点云密度较均匀、场景尺度可控，效果很好

- **缺点**：
  - cellSize 选择敏感：太大候选多，太小内存与构建成本高
  - 如果点云密度极不均匀，可能退化
  - 依赖 `pickWorldPosition` 会受穿透影响；用射线查询则实现更复杂

- **改动程度**：中
- **涉及文件**：
  - `packages/engine/Source/Scene/GaussianSplatPrimitive.js`
  - 建议新增：`packages/engine/Source/Scene/GaussianSplatSpatialHash.js`
- **接入点**：同 B1（替换候选集生成）

---

### B3. 屏幕空间桶（Screen-space Binning）（适合“相机不频繁变化/允许预处理”的交互）

- **核心思路**：
  - 当相机/viewport 改变时，做一次“全量投影”，把每个点落入一个屏幕格子（例如 64×64 像素 bin）
  - 每个 bin 存点索引列表（或直接存“最接近中心的前 K 个点”）
  - 点击时只取当前点击 bin（以及邻近 bins）里少量点进行精筛

- **优点**：
  - 点击查询接近 O(1)，非常快
  - 与 `maxDistance`（像素阈值）天然一致

- **缺点**：
  - 相机每变化一次都要重建 bin（等价把 O(N) 从“点击时”搬到“相机变更时”）
  - 视角频繁变化（拖拽旋转）会很重，需要做节流/降采样/后台构建

- **改动程度**：中到大
- **涉及文件**：
  - `packages/engine/Source/Scene/GaussianSplatPrimitive.js`
  - （可选）新增屏幕桶管理模块
- **接入点**：
  - 在 `update(frameState)`：检测相机变化时重建/增量更新 bins
  - 在 `getSplatInfoAtScreenPosition`：用 bins 生成候选集

---

## 方案 C：GPU picking（对照方案，性能最佳）

这一类方案本质上是：让 GPU 帮你决定“点击像素对应的 splat id”，CPU 不做全量扫描。

### C1. 离屏渲染 pick pass：把 plyIndex 编码进颜色，readPixels 解码

- **核心思路**（与 `GPU拾取优化实现方案分析.md` 一致）：
  - pick pass 下，顶点/片元着色器输出 `plyIndex` 编码色
  - 渲染到离屏 framebuffer
  - `readPixels` 读取点击像素，解码得到 plyIndex
  - 用 `_plyIndexToAggregateIndex` 映射回 aggregateIndex，组装 result

- **优点**：
  - 点击时间复杂度接近 O(1)
  - 不依赖 `searchSelectedOnly`，天然覆盖全量点位
  - 对超大点数最友好

- **缺点**：
  - 实现与维护成本更高（FBO、passState、WebGL 兼容）
  - `readPixels` 在很多环境是同步阻塞（但通常远小于 CPU 全量扫）
  - 需要处理遮挡/深度/透明等规则（你们方案里已明确 pick pass 关闭深度或调整）

- **改动程度**：中偏大
- **涉及文件（按既有方案文档）**：
  - `packages/engine/Source/Scene/GaussianSplatPrimitive.js`
  - `packages/engine/Source/Shaders/PrimitiveGaussianSplatVS.glsl`
  - `packages/engine/Source/Shaders/PrimitiveGaussianSplatFS.glsl`
  - （可选）其它渲染资源/Pass 管理文件
- **接入点**：
  - 在 `getSplatInfoAtScreenPosition` 开头：当允许 GPU picking 时走 `_getSplatInfoAtScreenPositionGpu(...)` 分支

> 说明：你仓库当前 `GaussianSplatPrimitive.js` 已有 pick 相关字段与 `PICK_PASS` shader define 痕迹，但本文档作为“全量优化方案对照”只描述实现方向与改动程度，具体实现细节以 `GPU拾取优化实现方案分析.md` 为准。

---

## 方案对比（优缺点 + 改动程度）

| 方案 | 覆盖全量点位 | 点击耗时 | 首次/更新成本 | 改动程度 | 主要风险 |
|---|---|---|---|---|---|
| A1 缓存/去掉全量数组 | 是 | 仍 O(N)（常数更小） | 无 | 小 | 收益有限 |
| A2 平方距离 | 是 | 仍 O(N)（常数更小） | 无 | 小 | 收益依赖热点比例 |
| A3 分帧/Worker | 是 | 不阻塞（总耗时不一定小） | 数据拷贝/调度 | 中 | API 形态变化 |
| A4 手写投影 | 是 | 仍 O(N)（常数可能更小） | 无 | 中 | 兼容性/正确性风险 |
| B1 Octree | 是 | 近似 O(log N + K) | 建树 O(N) | 中到大 | 内存 + 参数调优 |
| B2 均匀网格 | 是 | 近似 O(1~K) | 建格 O(N) | 中 | cellSize 敏感 |
| B3 屏幕桶 | 是 | 近似 O(1~K) | 相机变化时 O(N) | 中到大 | 相机频繁变化退化 |
| C1 GPU picking | 是 | 接近 O(1) | 维护 pick pass | 中偏大 | WebGL/readPixels/遮挡规则 |

---

## 推荐实施顺序（按“改动/收益比”）

### 推荐 1：先做 A1 + A2（马上见效，风险低）
- 先把“每次点击创建全量数组”和“sqrt”这两类纯开销去掉
- 改动小、回归测试容易

### 推荐 2：如果仍卡顿，选 B1（Octree）或 C1（GPU picking）
- **优先 B1**：更偏 CPU 算法层，便于调试，且能保留现有精筛规则
- **追求极致交互**：选 C1（GPU picking），对超大点数更稳定

### 推荐 3：如果你有“相机基本不动、频繁点选”的场景
- B3 屏幕桶的体感会非常好（点击极快），但要做好相机变化时的节流/降采样

---

## 修改代码程度（给实施评估用）

### 改动小（1-2 天级别，主要是局部重构）
- 只改 `packages/engine/Source/Scene/GaussianSplatPrimitive.js`
- 不新增模块/不改 shader
- 典型：A1、A2

### 改动中（数天到 1-2 周，涉及新模块与生命周期）
- 新增 `GaussianSplatOctree.js` / `GaussianSplatSpatialHash.js`，并在 `GaussianSplatPrimitive` 接入
- 典型：B1、B2、A3（若引入 worker）

### 改动中偏大（1-3 周，渲染管线 + shader + FBO）
- `GaussianSplatPrimitive.js` + 2 个 shader 的 pick pass 设计与 readPixels
- 典型：C1（参考 `GPU拾取优化实现方案分析.md`）

